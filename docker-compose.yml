services:
  db:
    image: postgres:14-alpine
    ports:
      - '15001:5432'
    restart: 'no'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=secret99
      - POSTGRES_DB=temani_db
    volumes: 
      - db:/var/lib/postgresql/data
volumes:
  db:
    driver: local